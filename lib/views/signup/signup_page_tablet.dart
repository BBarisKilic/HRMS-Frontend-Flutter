import 'package:flutter/material.dart';

class SignupPageTablet extends StatelessWidget {
  const SignupPageTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
